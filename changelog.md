

# v0.0.511
- Аналітика: повна історія на графіках — слайдер прогнозу більше НЕ перемальовує графіки, лише блок прогнозу (ізольований updateForecastBlock)
- Аналітика: видалені чіпи «7д / 30д» — усі агреговані значення тепер у KPI-дашборді (Net / Avg/day / Max/day / Trend 7d) над графіками
- Аналітика: блок прогнозу тепер показує дату-орієнтир (DD.MM.YYYY), сценарії Песиміст/Оптиміст (мін/макс за 30 днів) та акцентну картку
- Аналітика: перемикач режимів графіка [Накопичувальний | Денні зміни] з нульовою лінією та tooltip (дата, зміна, баланс)
- Слайдер прогнозу: діапазон 1..90 днів
- Збереження режиму графіка та періоду прогнозу в localStorage

# v0.0.510
- Аналітика: модальні вікна Скидання та Імпорту тепер закриваються клавішею Escape (раніше — лише ✕ і клік по фону)
- Аналітика: темп прогнозу 🟢 +X / день — фіксований зелений колір (#34d399), не зливається з кольором суми у жодній темі
- Аналітика: від'ємний темп показується 🔴 червоним, нульовий — «— / день» (без неінформативного «+0»)
- Layout: padding-bottom .wrap враховує safe-area-inset-bottom у portrait, tab-bar не перекриває останні картки на пристроях з індикатором Home
- Імпорт: специфічний ключ помилки «an_imp_error» замість копіювального changelog-плейсхолдера; переклад uk + en

# v0.0.509
- Аналітика: видалено блок «Підсумки активності» та перемикач періодів — один надійний розрахунок доходу по останніх 14 днях
- Аналітика: кольорова ієрархія прогнозу — сума в кольорі ресурсу (Cash зелений, Gold золотистий, Престиж фіолетовий, Слоти блакитний), назва нейтральна, темп 🟢 дрібним зеленим
- Кнопка статистики скорочена до компактного стикера «N дн.»
- Модальні вікна: сучасний напівпрозорий дизайн, кнопка закриття ✕ та клік по фону — для вікон установки, скидання аналітики та імпорту
- 


# v0.0.508-beta
- ANALYTICS: export/import backup JSONL, validation, merge/overwrite modal, instant refresh
- ANALYTICS: income window segmented control 7/14/30 days, dynamic recalc
- ANALYTICS: activity highlights - peak day and week-over-week trend (%)
# v0.0.507
- Аналітика: кнопку «Очистити» замінено на інтерактивний стикер «Статистика: N дн.» (кількість днів збору від першого знімка)
- Аналітика: скидання історії тепер через модальне вікно підтвердження («Скидання історії аналітики» з датою початку збору та червоною кнопкою «🗑️ Скинути дані») — без миттєвого видалення
- Після підтвердження лічильник скидається на «Статистика: 0 дн.» із оновленням графіків і прогнозу
- 
# v0.0.506
- Аналітика (нова модель): прогноз = лінійне накопичення ЧИСТОГО ДОХОДУ — Forecast = Balance + Income/день × Days; одноразові витрати минулих днів не створюють «щоденний мінус»
- Аналітика: прибрано Net Rate, червоні підкладки та бейджі «вичерпається» — прогноз завжди показує ріст від поточного залишку
- Аналітика: 🛍️ «Витрата: −X (дата)» — довідковий показник останнього списання
- Картка прогнозу: Поточний баланс → Прогноз на N дн. → 🟢 Темп прибутку +X / день

# v0.0.505-beta
- Журнал (критичний фікс): контейнер логів більше не «протікає» на інші вкладки — display лише в активній вкладці Журналу, з розтягом до таб-бару
- Аналітика: картки прогнозу спрощено — головне число = прогнозований баланс, під ним чистий Net Rate (+X/−Y на добу); брутто-деталі (дохід/витрати) у підказці рядка
- Аналітика: таймер вичерпання рахується по NET-темпу (Income − Expenses): Days_left = floor(Баланс / |Net|); бейдж лише коли період прогнозу ≥ Days_left

# v0.0.504-beta
- Аналітика: середньодобовий приріст рахується ТІЛЬКИ по позитивних дельтах (чистий заробіток); витрати винесено окремим показником та використовуються для таймера вичерпання
- Журнал: контейнер розтягується до нижнього таб-бару (calc(100vh)), максимальна корисна площа
- Оновлення: кнопка «Завантажити» тепер реально встановлює модуль — завантаження zip, перевірка SHA-256, встановлення через magisk/ksud зі стильним overlay-прогресом і логом кроків
- Метадані: лаконічна назва TD2TDR Sync та короткий опис
- Landscape: повноцінна адаптація — двоколонкові Гараж/Аналітика, компактний таб-бар, Журнал на всю висоту

# v0.0.503-beta
- Аналітика (критичний фікс): усунено вічне завантаження — відновлено читання history.jsonl з фолбеком на /data/media, повний try/catch та повідомлення про помилку замість зависання
- Журнал: збільшено висоту вікна (300px+ / 58vh), кольорова індикація рівнів (INFO блакитний / WARN помаранчевий / ERR червоний / DEBUG сірий), миготіння таба «Журнал» червоним при помилках
- Гараж: видалено donut-діаграму — лишився один чистий стек-розподіл прокачки; картки повністю мінімалістичні без акцентних рамок
- td2tdr_sync: locale/theme/ui_lang у localStorage, файли-конфіги автовидаляються з папки

# v0.0.502-beta
- Аналітика: коректний від'ємний прогноз (Bal + Delta×Days, кламп на 0), бейдж «вичерпається за N дн.» тільки коли період прогнозу сягає моменту обнулення
- Аналітика: «Піковий день витрат: -X (дата)» поруч із піковим приростом; кольорова індикація добового приросту та «зона ризику» для від'ємних прогнозів
- Гараж: мінімалістичні уніфіковані картки — без важких градієнтів і внутрішніх світінь
- td2tdr_sync: locale/theme/ui_lang перенесено в localStorage WebUI, файли-конфіги видаляються з папки (лишаються лише Garage.dat, user.dat, history.jsonl)

# v0.0.501
- Синхронізація:
  - Миттєве завантаження останнього стану з кешу localStorage при відкритті WebUI (без «Не завантажено» та очікування shell-команд);
  - Видалено фонові авто-запити при запуску — повторне читання/копіювання тільки за кнопками «Синхронізувати та відкрити» / «Оновити»;
  - Ретраї верифікації з паузами, sync кешу ФС, fallback-копіювання у /storage при блокуванні /data/media;
  - Детальне логування кожного кроку копіювання в sync.log.
- Гараж:
  - Контрастний редезайн карток: акцентні градієнти та рамки для блоків «Прокачка» і «Бої»;
  - Компактна кругова діаграма (donut) розподілу прокачки;
  - Світлодіодне попередження слотів при заповненні >95%;
  - Виділений тег Held з поясненням.
- Аналітика:
  - Прогнозний слайдер періоду 3–90 днів замість фіксованих кнопок;
  - Двоколонковий компактний блок прогнозу із середньодобовим приростом (+X / день);
  - Індикатор точності прогнозу за обсягом накопиченої історії;
  - Метрика «Гараж» рахується по зайнятих слотах; «Піковий день» замість «Рекорду приросту».
- Оптимізація:
  - Зачистка коду, видалення тимчасових артефактів;
  - Кешування статусу, липкі зелений стан без хибних скидань;
  - changelog.md вшито в білд + локальний fallback вікна «Історія версій»;
  - Підвищення стабільності та оптимізація розміру модуля.


# v0.0.305
- Виправлено помилку сайту «NOT valid JSON» при виборі Garage.dat:
  - Створено єдиний скрипт синхронізації `sync_now.sh` для фонового демона та WebUI.
  - Копіювання через прямий шлях `/data/media/0/Download/td2tdr_sync` з призначенням прав `media_rw` (uid/gid 1023) та контексту `media_rw_data_file` — браузер тепер гарантовано читає файл.
  - Атомарний запис через `.part` з очікуванням стабільного розміру джерела (усунено гонку запису під час збереження гри).
  - Примусове сповіщення MediaStore (`am broadcast / content scan`) для моментальної видимості файлу в системному провіднику.
  - Прибрано подію `modify` з `inotifywait` (залишено тільки `close_write`, `create`, `moved_to`).
  - Виправлено збій `chmod` у WebUI при відсутності `user.dat`.

# v0.0.302