# v0.0.001
- Перший реліз після повного перезапуску версій (було 1.3.018 → стало 0.0.001).
- Налаштовано GitHub Actions: автореліз за тегом `v*`, заповнення `update.json` (zipUrl + sha256), генерація changelog.
- Додано README з повною інструкцією.
- Очищено проєкт від дублікатів (banner/icon/pngs).
- Анімована кнопка синхронізації у WebUI.
- Додано дисклеймер про відповідальність на початку README.
- Додано розділ README «Банер модуля»: параметри (1024×500 PNG) та AI-промт для генерації.
- Видалено непотрібний `banner.webp` (використовується тільки `banner.png`).
- Оновлено банер модуля (новий дизайн, 1024×500 PNG).
- Перероблено інсталяційний процес у `customize.sh`:
  - красивий вивід з рамками;
  - дисклеймер про відповідальність (AS IS);
  - підтвердження встановлення: [Volume+] Так / [Volume-] Ні, за замовчуванням — Ні (скасування);
  - при відмові модуль скасовується без залишків.
- Перевипущено реліз (оновлено changelog, перепаковано архів, оновлено sha256 в `update.json`).
